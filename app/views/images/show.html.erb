<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @image.user.email %>
</p>

<p>
  <span><%= image_tag(@image.file.url(:medium)) %></span>
</p>

<p>
  <strong>File name:</strong>
  <%= @image.file_file_name %>
</p>

<%= link_to 'Destroy', @image, method: :delete, data: { confirm: 'Are you sure?' } %>
